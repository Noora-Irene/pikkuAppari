class Marker {
    constructor(latitude, longitude, title, pinColor) {
        this.latitude= latitude;
        this.longitude= longitude;
        this.title=title;
        this.pinColor=pinColor;
    }
}

export default Marker;